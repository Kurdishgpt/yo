# Backend API Configuration
# Leave empty for local development (same server on port 5000)
# Set to your deployed backend URL for production (e.g., Railway, Render)
# Example: VITE_API_URL=https://your-app.railway.app
VITE_API_URL=

# Database (for backend only)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
